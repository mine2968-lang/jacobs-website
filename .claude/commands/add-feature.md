Add the following feature to the to-do app in index.html: $ARGUMENTS

Keep everything in the single index.html file. Match the existing code style, color scheme (#6366f1 accent), and the save()/render() pattern already used. Do not introduce external dependencies.
